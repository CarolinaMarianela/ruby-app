
  <h1 class="title-receta"><%= @article.title %></h1>

  <p class="body-text"><%= @article.body %></p>
<div class="page-receta">
  <ul class="list-element">
    <li><%= link_to "Editar", edit_article_path(@article) %></li>
    <li><%= link_to "Eliminar", article_path(@article), data: {
                      turbo_method: :delete,
                      turbo_confirm: "Estas seguro que deseas eliminar esta receta?"
                    } %></li>
  </ul>

  <h2>Deja tu comentario</h2>
  <%= render @article.comments %>

  <h2>Añade un comentario:</h2>
  <%= render 'comments/form' %>
</div>

